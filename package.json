{
  "scripts": {
    "build": "imba-rollup",
    "start": "imba-rollup --watch --serve --hmr",
    "check-version": "imbac --version"
  },
  "dependencies": {
    "bulma": "^0.8.0",
    "imba": "^2.0.0-alpha.20",
    "imba-rollup": "^1.0.3"
  },
  "devDependencies": {}
}
